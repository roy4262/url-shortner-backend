# Example environment variables for TinyLink backend
# Replace values with your deployment-specific values.

# PostgreSQL connection string (required)
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Public base URL used to construct short URLs (e.g. https://tiny.example.com)
# If not set, the server will derive the host from incoming requests.
SHORT_URL_BASE=https://your-deployed-domain.example.com

# Restrict CORS origin for production (set to your frontend origin). Use '*' for open.
CORS_ORIGIN=*

# Port to run locally
PORT=4000

# Optional: set to true to enable verbose DB query logging
# VERBOSE_DB=false
